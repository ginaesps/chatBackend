file_path: /chatBackend/src/main/resources/

server:
  port: 9090

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/chatapp
    username: root
    password: regina123
    driver-class-name: com.mysql.cj.jdbc.Driver

  jpa:
    show-sql: true
    hibernate:
      ddl-auto: update
    properties:
      hibernate:
        dialect: org.hibernate.dialect.MySQL5InnoDBDialect

logging:
  level:
    root: INFO
    org.hibernate: DEBUG






